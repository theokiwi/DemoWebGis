import type { FacilityFeature } from '../api/client';
export function FacilityList({facilities,onSelect}:{facilities:FacilityFeature[];onSelect:(id:string)=>void}) { return <section className="facility-list"><h2>Estabelecimentos no recorte <span>{facilities.length}</span></h2>{facilities.length===0?<p>Nenhum estabelecimento atende aos filtros. Restaure as categorias para continuar.</p>:<ul>{facilities.map(item=><li key={item.id}><button onClick={()=>onSelect(item.id)}><strong>{item.properties.name}</strong><span>{item.properties.categories.join(', ')} · {item.properties.servesSus?'Atende SUS':'Oferta privada'}</span></button></li>)}</ul>}</section>; }
